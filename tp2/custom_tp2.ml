(* 
  Exercice 1
  Inserts e in a sorted list using the comparaison function ord_func
  Inputs:
    - ord_func: 'a '-> 'a '-> bool; ordering function.
    - e: 'a; element to insert
    - sorted_list: 'a list; the sorted list in which e should be inserted
*)
(* let insert ord_func e sorted_list = List.fold_right fun h q -> if ord_func h e  *)